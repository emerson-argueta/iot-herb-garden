# Mosquitto MQTT broker — herb garden
# Local-only: no TLS, no auth (add password_file for production hardening).

listener 1883
allow_anonymous true

# Retain messages so clients receive the last garden/state on connect.
persistence true
persistence_location /mosquitto/data/

log_dest stdout
log_type error
log_type warning
log_type notice
log_type information
